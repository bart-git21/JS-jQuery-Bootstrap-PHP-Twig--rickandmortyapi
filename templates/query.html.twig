{
  characters(page: 2, filter: { name: "rick" }) {
    info {
      count
    }
    results {
      name
    }
  }
  location(id: 1) {
    id
    name
  }
  episodesByIds(ids: [{% for id in ids %} {{ id }}, {% endfor %}]) {
    id
    name
  }
}